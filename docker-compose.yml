services:
  web:
    image: ${DOCKER_USERNAME}/easy_shop
    build:
      context: .
      dockerfile: src/API/Dockerfile
    ports:
      - "5000:5000"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
    restart: always
    volumes:
      - ./uploads:/app/wwwroot/uploads
